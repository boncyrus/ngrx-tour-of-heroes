import { AvatarInitialPipe } from './avatar-initial.pipe';

describe('AvatarInitialPipe', () => {
    it('create an instance', () => {
        const pipe = new AvatarInitialPipe();
        expect(pipe).toBeTruthy();
    });
});
